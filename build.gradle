apply plugin: 'java'


//java {
//    toolchain {
//        languageVersion = JavaLanguageVersion.of(11)
//    }
//}

sourceCompatibility = 1.11
targetCompatibility = 1.11 

archivesBaseName = "ACE"
version = '2.6'
 
repositories {
 mavenCentral()
}

dependencies {
    implementation fileTree(dir: 'lib', include: ['*.jar'])
    implementation 'org.ojalgo:ojalgo:49.2.1'
    //testImplementation 'junit:junit:4.12'
}

// with this statement below (jar { }), we build a fat jar, i.e. a jar with included dependencies
jar {
    dependsOn testClasses
    manifest {
        attributes(
                'Main-Class': 'main.Head'
        )
    }
    from sourceSets.test.output
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
 }


test {
	//executable = "/path/to/jdk/bin/java"
	
   // show standard out and standard error of the test JVM(s) on the console
  testLogging.showStandardStreams = true
 
   
 // set heap size for the test JVM(s)
  minHeapSize = "128m"
  maxHeapSize = "1024m"
  
  // set JVM arguments for the test JVM(s)
  jvmArgs "-da" 
  jvmArgs "-Xmx12G"  // -d64 
}

task benchmarkLpVsNoLp(type: JavaExec) {
	group = "verification"
	description = "Benchmark ACE with LP enabled and disabled on the same optimization instances, focusing on proof, gap and nodes."
	dependsOn testClasses
	classpath = sourceSets.test.runtimeClasspath
	mainClass = "problems.BenchmarkLpVsNoLp"
	jvmArgs "-da"
	jvmArgs "-Xmx12G"
	if (project.hasProperty("benchmarkArgs")) {
		args project.property("benchmarkArgs").toString().trim().split("\\s+").findAll { !it.isEmpty() }
	}
}
